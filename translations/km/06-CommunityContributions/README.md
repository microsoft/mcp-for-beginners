# សហគមន៍ និងការរួមចំណែក

[![របៀបរួមចំណែកទៅ MCP: ឧបករណ៍, ឯកសារ, កូដ និងបន្ថែមទៀត](../../../translated_images/km/07.1179f6de46ff196e.webp)](https://youtu.be/v1pvCYAWpRE)

_(ចុចលើរូបភាពខាងលើដើម្បីមើលវីដេអូមេរៀននេះ)_

## សង្ខេប

មេរៀននេះផ្តោតលើរបៀបអញ្ចើញចូលរួមក្នុងសហគមន៍ MCP, រួមចំណែកទៅក្នុងប្រព័ន្ធ MCP, និងតាមដានអនុសាសន៍ល្អបំផុតសម្រាប់ការអភិវឌ្ឍរួមគ្នា។ ការយល់ដឹងពីរបៀបចូលរួមក្នុងគម្រោង MCP ប្រភពបើកគឺសំខាន់សម្រាប់អ្នកដែលចង់បង្កើតអនាគតនៃបច្ចេកវិទ្យានេះ។

## វិស័យសិក្សា

នៅចុងបញ្ចប់មេរៀននេះ អ្នកនឹងអាច:

- យល់ដឹងពីរចនាសម្ព័ន្ធនៃសហគមន៍ និងប្រព័ន្ធសេដ្ឋកិច្ច MCP
- ចូលរួមយ៉ាងមានប្រសិទ្ធភាពក្នុងកម្មវិធីសហគមន៍ MCP និងការពិភាក្សា
- រួមចំណែកទៅក្នុងឃ្លាំងប្រភពបើក MCP
- បង្កើតនិងចែករំលែកឧបករណ៍ និងម៉ាស៊ីនបម្រើ MCP ផ្ទាល់ខ្លួន
- តាមដានអនុសាសន៍ល្អបំផុតសម្រាប់ការអភិវឌ្ឍ និងការសហការក្នុង MCP
- រកឃើញធនធាន និងស៊ុមសម្រាប់ការអភិវឌ្ឍ MCP

## ប្រព័ន្ធសេដ្ឋកិច្ចសហគមន៍ MCP

ប្រព័ន្ធសេដ្ឋកិច្ច MCP មានអង្គភាពនិងអ្នកចូលរួមជាច្រើនដែលធ្វើការជាមួយគ្នាដើម្បីជំរុញវិធានការ។

### ធាតុសំខាន់នៃសហគមន៍

1. **អ្នកថែទាំវិធានការ​កម្រិត​មូលដ្ឋាន**៖ អង្គការដើម [Model Context Protocol GitHub](https://github.com/modelcontextprotocol) ធ្វើការថែទាំលិខិតបញ្ជាក់មូលដ្ឋាន MCP និងការអនុវត្តតំណាង
2. **អ្នកអភិវឌ្ឍឧបករណ៍**៖ បុគ្គល និងក្រុមដែលបង្កើតឧបករណ៍ និងម៉ាស៊ីនបម្រើ MCP
3. **អ្នកផ្តល់សេវាកម្មបញ្ចូល**៖ ក្រុមហ៊ុនដែលបញ្ចូល MCP ទៅក្នុងផលិតផល និងសេវាកម្មរបស់ពួកគេ
4. **អ្នកប្រើប្រាស់ចុងក្រោយ**៖ អ្នកអភិវឌ្ឍន៍ និងអង្គការដែលប្រើ MCP ក្នុងកម្មវិធីរបស់ពួកគេ
5. **អ្នករួមចំណែក**៖ សមាជិកសហគមន៍ដែលរួមចំណែកកូដ អ្នកឯកសារ ឬធនធានផ្សេងទៀត

### ធនធានសហគមន៍

#### បណ្ដាញផ្លូវការ

- [អង្គការមា្មន MCP GitHub](https://github.com/modelcontextprotocol)
- [ឯកសារមា្មន MCP](https://modelcontextprotocol.io/)
- [លិខិតបញ្ជាក់មា្មន MCP](https://modelcontextprotocol.io/specification/2026-07-28/)
- [ការពិភាក្សានៅ GitHub](https://github.com/orgs/modelcontextprotocol/discussions)
- [ឃ្លាំងឧទាហរណ៍ & ម៉ាស៊ីនបម្រើ MCP](https://github.com/modelcontextprotocol/servers)

#### ធនធានជំនួយពីសហគមន៍

- [អតិថិជន MCP](https://modelcontextprotocol.io/clients) - បញ្ជីអតិថិជនដែលគាំទ្រការបញ្ចូល MCP
- [ម៉ាស៊ីនបម្រើ MCP សហគមន៍](https://github.com/modelcontextprotocol/servers?tab=readme-ov-file#-community-servers) - បញ្ជីកើនឡើងនៃម៉ាស៊ីនបម្រើ MCP ដែលបានអភិវឌ្ឍដោយសហគមន៍
- [ម៉ាស៊ីនបម្រើ MCP អស្ចារ្យ](https://github.com/wong2/awesome-mcp-servers) - បញ្ជីវាយតម្លៃនៃម៉ាស៊ីនបម្រើ MCP
- [PulseMCP](https://www.pulsemcp.com/) - មជ្ឈមណ្ឌលសហគមន៍ និងសេចក្តីជូនដំណឹងសម្រាប់ការរកឃើញធនធាន MCP
- [Remote OpenClaw](https://www.remoteopenclaw.com/) - បញ្ជីស្វែងរកដោយឥតគិតថ្លៃនៃម៉ាស៊ីនបម្រើ MCP, ជំនាញភ្នាក់ងារ និងផ្លាកឈ្មោះបន្ថែម
- [ម៉ាស៊ីនបម្រើ Discord](https://discord.gg/jHEGxQu2a5) - ភ្ជាប់ជាមួយអ្នកអភិវឌ្ឍ MCP
- ការអនុវត្ត SDK ជាភាសាផ្សេងៗ
- ការបង្ហោះប្លុក និងមេរៀន

## ការរួមចំណែកទៅ MCP

### ប្រភេទការរួមចំណែក

ប្រព័ន្ធសេដ្ឋកិច្ច MCP ស្វាគមន៍ប្រភេទការរួមចំណែកជាច្រើន:

1. **ការរួមចំណែកកូដ**:
   - ការកែលម្អកម្មវិធីមូលដ្ឋាន
   - ការជួសជុលកំហុស
   - ការអនុវត្តឧបករណ៍ និងម៉ាស៊ីនបម្រើ
   - បណ្ណាល័យអតិថិជន/ម៉ាស៊ីនបម្រើជាភាសាផ្សេងៗ

2. **ឯកសារ**:
   - កែលម្អឯកសារដែលមានស្រាប់
   - បង្កើតមេរៀននិងមគ្គុទេសក៍
   - ថតបំលែងឯកសារ
   - បង្កើតឧទាហរណ៍ និងកម្មវិធីសាកល្បង

3. **ការគាំទ្រសហគមន៍**:
   - ផ្តល់ចម្លើយសំណួរនៅលើកម្រិតម៉ោងនិងពិភាក្សា
   - សាកល្បងនិងរាយការណ៍បញ្ហា
   - រៀបចំព្រឹត្តិការណ៍សហគមន៍
   - វៃឆៃអ្នករួមចំណែកថ្មី

### ដំណើរការចូលរួម: វិធានការមូលដ្ឋាន

ដើម្បីរួមចំណែកទៅការផ្ដោតមេដឹកនាំ MCP ឬការអនុវត្តផ្លូវការនៅក្នុង [ជំហានចូលរួមផ្លូវការ](https://github.com/modelcontextprotocol/modelcontextprotocol/blob/main/CONTRIBUTING.md) សូមអនុវត្តតាមគោលការណ៍ទាំងនេះ៖

1. **ភាពសាមញ្ញ និងអប្បបរមា**៖ លិខិតបញ្ជាក់ MCP រក្សានូវកម្រិតខ្ពស់សម្រាប់ការបន្ថែមគំនិតថ្មី។ វាស្រួលបន្ថែមរឿងទៅលិខិតបញ្ជកជាងការដកចេញ។

2. **វិធីសាស្រ្តកន្រ្តាក់**៖ ការផ្លាស់ប្ដូរលិខិតបញ្ជកគួរត្រូវមានមូលដ្ឋានលើបញ្ហាការអនុវត្តជាក់លាក់ មិនមែនគិតជាផ្នែកជាមូលដ្ឋាន។

3. **ជំហាននៃសំណើ**:
   - កំណត់: ស្វែងយល់ពីលំហរការបញ្ហា បញ្ជាក់ថាអ្នកប្រើ MCP ផ្សេងទៀតក៏ជួបប្រទៈបញ្ហាដូចគ្នា
   - សំណុំផ្សេងៗ: បង្កើតដំណោះស្រាយ ធ្វើបង្ហាញការចេញអនុវត្តជាក់ស្តែង
   - សរសេរ: dựa trênផ្នែកសំណំសំណើ សរសេរលិខិតបញ្ជាក់

### ការតំឡើងបរិយាកាសអភិវឌ្ឍ

```bash
# បង្កើតសាខារបស់仓库
git clone https://github.com/YOUR-USERNAME/modelcontextprotocol.git
cd modelcontextprotocol

# តំឡើងបណ្ដាប់ dependency
npm install

# សម្រាប់ការប្រែប្រួល schema ផ្ទៀងផ្ទាត់ និងបង្កើត schema.json:
npm run check:schema:ts
npm run generate:schema

# សម្រាប់ការផ្លាស់ប្តូរឯកសារ
npm run check:docs
npm run format

# 미리보기 문서를 로컬에서 (ជម្រើស):
npm run serve:docs
```

### ឧទាហរណ៍: រួមចំណែកជួសជុលកំហុស

```javascript
// កូដដើមដែលមានកំហុសនៅក្នុង typescript-sdk
export function validateResource(resource: unknown): resource is MCPResource {
  if (!resource || typeof resource !== 'object') {
    return false;
  }
  
  // កំហុស៖ ខ្វះការផ្ទៀងផ្ទាត់លក្ខណៈ
  // ការអនុវត្តបច្ចុប្បន្នៈ
  const hasName = 'name' in resource;
  const hasSchema = 'schema' in resource;
  
  return hasName && hasSchema;
}

// ការអនុវត្តដែលបានកែប្រែ​ក្នុងការរួមចំណែក
export function validateResource(resource: unknown): resource is MCPResource {
  if (!resource || typeof resource !== 'object') {
    return false;
  }
  
  // ការផ្ទៀងផ្ទាត់កែលម្អ
  const hasName = 'name' in resource && typeof (resource as MCPResource).name === 'string';
  const hasSchema = 'schema' in resource && typeof (resource as MCPResource).schema === 'object';
  const hasDescription = !('description' in resource) || typeof (resource as MCPResource).description === 'string';
  
  return hasName && hasSchema && hasDescription;
}
```

### ឧទាហរណ៍: រួមចំណែកឧបករណ៍ថ្មីទៅបណ្ណាល័យស្តង់ដារ

```python
# ឧទាហរណ៍ការរួមចំណែក៖ ឧបករណ៍ដំណើរការទិន្នន័យ CSV សម្រាប់បណ្ណាល័យស្តង់ដារ MCP

from mcp_tools import Tool, ToolRequest, ToolResponse, ToolExecutionException
import pandas as pd
import io
import json
from typing import Dict, Any, List, Optional

class CsvProcessingTool(Tool):
    """
    Tool for processing and analyzing CSV data.
    
    This tool allows models to extract information from CSV files,
    run basic analysis, and convert data between formats.
    """
    
    def get_name(self):
        return "csvProcessor"
        
    def get_description(self):
        return "Processes and analyzes CSV data"
    
    def get_schema(self):
        return {
            "type": "object",
            "properties": {
                "csvData": {
                    "type": "string", 
                    "description": "CSV data as a string"
                },
                "csvUrl": {
                    "type": "string",
                    "description": "URL to a CSV file (alternative to csvData)"
                },
                "operation": {
                    "type": "string",
                    "enum": ["summary", "filter", "transform", "convert"],
                    "description": "Operation to perform on the CSV data"
                },
                "filterColumn": {
                    "type": "string",
                    "description": "Column to filter by (for filter operation)"
                },
                "filterValue": {
                    "type": "string",
                    "description": "Value to filter for (for filter operation)"
                },
                "outputFormat": {
                    "type": "string",
                    "enum": ["json", "csv", "markdown"],
                    "default": "json",
                    "description": "Output format for the processed data"
                }
            },
            "oneOf": [
                {"required": ["csvData", "operation"]},
                {"required": ["csvUrl", "operation"]}
            ]
        }
    
    async def execute_async(self, request: ToolRequest) -> ToolResponse:
        try:
            # ដកយកប៉ារ៉ាម៉ែត្រ
            operation = request.parameters.get("operation")
            output_format = request.parameters.get("outputFormat", "json")
            
            # ទទួលបានទិន្នន័យ CSV ពីទិន្នន័យថ្នាក់ដេរ ឬ URL
            df = await self._get_dataframe(request)
            
            # ដំណើរការតាមមុខងារដែលបានស្នើសុំ
            result = {}
            
            if operation == "summary":
                result = self._generate_summary(df)
            elif operation == "filter":
                column = request.parameters.get("filterColumn")
                value = request.parameters.get("filterValue")
                if not column:
                    raise ToolExecutionException("filterColumn is required for filter operation")
                result = self._filter_data(df, column, value)
            elif operation == "transform":
                result = self._transform_data(df, request.parameters)
            elif operation == "convert":
                result = self._convert_format(df, output_format)
            else:
                raise ToolExecutionException(f"Unknown operation: {operation}")
            
            return ToolResponse(result=result)
        
        except Exception as e:
            raise ToolExecutionException(f"CSV processing failed: {str(e)}")
    
    async def _get_dataframe(self, request: ToolRequest) -> pd.DataFrame:
        """Gets a pandas DataFrame from either CSV data or URL"""
        if "csvData" in request.parameters:
            csv_data = request.parameters.get("csvData")
            return pd.read_csv(io.StringIO(csv_data))
        elif "csvUrl" in request.parameters:
            csv_url = request.parameters.get("csvUrl")
            return pd.read_csv(csv_url)
        else:
            raise ToolExecutionException("Either csvData or csvUrl must be provided")
    
    def _generate_summary(self, df: pd.DataFrame) -> Dict[str, Any]:
        """Generates a summary of the CSV data"""
        return {
            "columns": df.columns.tolist(),
            "rowCount": len(df),
            "columnCount": len(df.columns),
            "numericColumns": df.select_dtypes(include=['number']).columns.tolist(),
            "categoricalColumns": df.select_dtypes(include=['object']).columns.tolist(),
            "sampleRows": json.loads(df.head(5).to_json(orient="records")),
            "statistics": json.loads(df.describe().to_json())
        }
    
    def _filter_data(self, df: pd.DataFrame, column: str, value: str) -> Dict[str, Any]:
        """Filters the DataFrame by a column value"""
        if column not in df.columns:
            raise ToolExecutionException(f"Column '{column}' not found")
            
        filtered_df = df[df[column].astype(str).str.contains(value)]
        
        return {
            "originalRowCount": len(df),
            "filteredRowCount": len(filtered_df),
            "data": json.loads(filtered_df.to_json(orient="records"))
        }
    
    def _transform_data(self, df: pd.DataFrame, params: Dict[str, Any]) -> Dict[str, Any]:
        """Transforms the data based on parameters"""
        # ការអនុវត្តនឹងរួមបញ្ចូលការបម្លែងផ្សេងៗ
        return {
            "status": "success",
            "message": "Transformation applied"
        }
    
    def _convert_format(self, df: pd.DataFrame, format: str) -> Dict[str, Any]:
        """Converts the DataFrame to different formats"""
        if format == "json":
            return {
                "data": json.loads(df.to_json(orient="records")),
                "format": "json"
            }
        elif format == "csv":
            return {
                "data": df.to_csv(index=False),
                "format": "csv"
            }
        elif format == "markdown":
            return {
                "data": df.to_markdown(),
                "format": "markdown"
            }
        else:
            raise ToolExecutionException(f"Unsupported output format: {format}")
```

### ទ្រឹស្តីបញ្ចូលរួមចំណែក

ដើម្បីធ្វើការរួមចំណែកជោគជ័យទៅគម្រោង MCP:

1. **ចាប់ផ្ដើមតូចៗ**: ចាប់ផ្ដើមដោយឯកសារ ជួសជុលកំហុស ឬកែលម្អតិចតួច
2. **អនុវត្តក្បួនស្ទីល**: ទំរង់តាមស្ទីល និងរៀបចំកូដនៃគម្រោង
3. **សរសេរប្រឡង**: រួមបញ្ចូលការប្រឡងឯកតាសម្រាប់កូដរបស់អ្នក
4. **ផ្តល់ឯកសារធ្វើការ**: បន្ថែមឯកសារបញ្ជាក់ច្បាស់លាស់សម្រាប់មុខងារថ្មី ឬការផ្លាស់ប្ដូរ
5. **ដាក់សំណើ PR តែមួយចំណុច**: រក្សាការស្នើសុំផ្ដិតពិនិត្យនៅលើបញ្ហា ឬមុខងារតែមួយ
6. **ចូលរួមជាមួយមតិយោបល់**: បទពិសោធន៍តបតាមមតិយោបល់លើការរួមចំណែករបស់អ្នក

### លំហូរប្រតិបត្ដិការរួមចំណែកឧទាហរណ៍

```bash
# ចម្លងឃ្លាំងទិន្នន័យ
git clone https://github.com/modelcontextprotocol/typescript-sdk.git
cd typescript-sdk

# បង្កើតសាខាថ្មីសម្រាប់ការរួមចំណែករបស់អ្នក
git checkout -b feature/my-contribution

# សូមធ្វើការផ្លាស់ប្តូររបស់អ្នក
# ...

# ប្រតិបត្តិការសាកល្បងដើម្បីធានាថាការផ្លាស់ប្តូររបស់អ្នកមិនបំផ្លាញមុខងារដែលមានរួចហើយ
npm test

# ប្តូរប្រែរបស់អ្នកជាមួយសារ​ពណ៌នាសង្ខេប
git commit -am "Fix validation in resource handler"

# ផ្តោតសាខារបស់អ្នកទៅកាន់សាខាដែលបានបំបែករបស់អ្នក
git push origin feature/my-contribution

# បង្កើតសំណើបញ្ចូលពីសាខារបស់អ្នកទៅឃ្លាំងទិន្នន័យមេ
# បន្ទាប់មកចូលរួមជាមួយមតិយោបល់ និងត្រឡប់ទៅកែលម្អ PR របស់អ្នកបើត្រូវការ
```

## បង្កើតនិងចែករំលែកម៉ាស៊ីនបម្រើ MCP

មួយក្នុងចំណោមវិធីមានតម្លៃបំផុតក្នុងការរួមចំណែកទៅប្រព័ន្ធសេដ្ឋកិច្ច MCP គឺការបង្កើតនិងចែករំលែកម៉ាស៊ីនបម្រើ MCP ផ្ទាល់ខ្លួន។ សហគមន៍បានអភិវឌ្ឍម៉ាស៊ីនបម្រើជាច្រើនសម្រាប់សេវាកម្មនិងករណីប្រើប្រាស់ផ្សេងៗ។

### ស៊ុមអភិវឌ្ឍម៉ាស៊ីនបម្រើ MCP

មានស៊ុមជាច្រើនសម្រាប់ធ្វើឲ្យការអភិវឌ្ឍម៉ាស៊ីនបម្រើ MCP ងាយស្រួល:

1. **SDK ផ្លូវការ** (ពិនិត្យ
    [ឯកសារ SDK](https://modelcontextprotocol.io/docs/sdk) សម្រាប់ភាពគាំទ្រលើកំណែសំណើ
    នីមួយៗ):
   - [TypeScript SDK](https://github.com/modelcontextprotocol/typescript-sdk)
   - [Python SDK](https://github.com/modelcontextprotocol/python-sdk)
   - [C# SDK](https://github.com/modelcontextprotocol/csharp-sdk)
   - [Go SDK](https://github.com/modelcontextprotocol/go-sdk)
   - [Java SDK](https://github.com/modelcontextprotocol/java-sdk)
   - [Kotlin SDK](https://github.com/modelcontextprotocol/kotlin-sdk)
   - [Swift SDK](https://github.com/modelcontextprotocol/swift-sdk)
   - [Rust SDK](https://github.com/modelcontextprotocol/rust-sdk)

2. **ស៊ុមសហគមន៍**:
   - [MCP-Framework](https://mcp-framework.com/) - បង្កើតម៉ាស៊ីនបម្រើ MCP យ៉ាងគូរនិងរហ័សនៅក្នុង TypeScript
   - [MCP Declarative Java SDK](https://github.com/codeboyzhou/mcp-declarative-java-sdk) - ម៉ាស៊ីនបម្រើ MCP ដោយអannotation ជាមួយ Java
   - [Quarkus MCP Server SDK](https://github.com/quarkiverse/quarkus-mcp-server) - ស៊ុម Java សម្រាប់ម៉ាស៊ីនបម្រើ MCP
   - [Next.js MCP Server Template](https://github.com/vercel-labs/mcp-for-next.js) - គំរូគម្រោង Next.js សម្រាប់ម៉ាស៊ីនបម្រើ MCP

### ការអភិវឌ្ឍឧបករណ៍ចែករំលែក

#### ឧទាហរណ៍ .NET: បង្កើតកញ្ចប់ឧបករណ៍ចែករំលែក

```csharp
// Create a new .NET library project
// dotnet new classlib -n McpFinanceTools

using Microsoft.Mcp.Tools;
using System.Threading.Tasks;
using System.Net.Http;
using System.Text.Json;

namespace McpFinanceTools
{
    // Stock quote tool
    public class StockQuoteTool : IMcpTool
    {
        private readonly HttpClient _httpClient;
        
        public StockQuoteTool(HttpClient httpClient = null)
        {
            _httpClient = httpClient ?? new HttpClient();
        }
        
        public string Name => "stockQuote";
        public string Description => "Gets current stock quotes for specified symbols";
        
        public object GetSchema()
        {
            return new {
                type = "object",
                properties = new {
                    symbol = new { 
                        type = "string",
                        description = "Stock symbol (e.g., MSFT, AAPL)" 
                    },
                    includeHistory = new { 
                        type = "boolean",
                        description = "Whether to include historical data",
                        default = false
                    }
                },
                required = new[] { "symbol" }
            };
        }
        
        public async Task<ToolResponse> ExecuteAsync(ToolRequest request)
        {
            // Extract parameters
            string symbol = request.Parameters.GetProperty("symbol").GetString();
            bool includeHistory = false;
            
            if (request.Parameters.TryGetProperty("includeHistory", out var historyProp))
            {
                includeHistory = historyProp.GetBoolean();
            }
            
            // Call external API (example)
            var quoteResult = await GetStockQuoteAsync(symbol);
            
            // Add historical data if requested
            if (includeHistory)
            {
                var historyData = await GetStockHistoryAsync(symbol);
                quoteResult.Add("history", historyData);
            }
            
            // Return formatted result
            return new ToolResponse {
                Result = JsonSerializer.SerializeToElement(quoteResult)
            };
        }
        
        private async Task<Dictionary<string, object>> GetStockQuoteAsync(string symbol)
        {
            // Implementation would call a real stock API
            // This is a simplified example
            return new Dictionary<string, object>
            {
                ["symbol"] = symbol,
                ["price"] = 123.45,
                ["change"] = 2.5,
                ["percentChange"] = 1.2,
                ["lastUpdated"] = DateTime.UtcNow
            };
        }
        
        private async Task<object> GetStockHistoryAsync(string symbol)
        {
            // Implementation would get historical data
            // Simplified example
            return new[]
            {
                new { date = DateTime.Now.AddDays(-7).Date, price = 120.25 },
                new { date = DateTime.Now.AddDays(-6).Date, price = 122.50 },
                new { date = DateTime.Now.AddDays(-5).Date, price = 121.75 }
                // More historical data...
            };
        }
    }
}

// Create package and publish to NuGet
// dotnet pack -c Release
// dotnet nuget push bin/Release/McpFinanceTools.1.0.0.nupkg -s https://api.nuget.org/v3/index.json -k YOUR_API_KEY
```

#### ឧទាហរណ៍ Java: បង្កើតកញ្ចប់ Maven សម្រាប់ឧបករណ៍

```java
// ការកំណត់រចនាសម្ព័ន្ធ pom.xml សម្រាប់កញ្ចប់ឧបករណ៍ MCP ដែលអាចចែករំលែកបាន
<!-- 
<project>
    <groupId>com.example</groupId>
    <artifactId>mcp-weather-tools</artifactId>
    <version>1.0.0</version>
    
    <dependencies>
        <dependency>
            <groupId>com.mcp</groupId>
            <artifactId>mcp-server</artifactId>
            <version>1.0.0</version>
        </dependency>
    </dependencies>
    
    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub Packages</name>
            <url>https://maven.pkg.github.com/username/mcp-weather-tools</url>
        </repository>
    </distributionManagement>
</project>
-->

package com.example.mcp.weather;

import com.mcp.tools.Tool;
import com.mcp.tools.ToolRequest;
import com.mcp.tools.ToolResponse;
import com.mcp.tools.ToolExecutionException;

import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.net.URI;
import java.util.HashMap;
import java.util.Map;

public class WeatherForecastTool implements Tool {
    private final HttpClient httpClient;
    private final String apiKey;
    
    public WeatherForecastTool(String apiKey) {
        this.httpClient = HttpClient.newHttpClient();
        this.apiKey = apiKey;
    }
    
    @Override
    public String getName() {
        return "weatherForecast";
    }
    
    @Override
    public String getDescription() {
        return "Gets weather forecast for a specified location";
    }
    
    @Override
    public Object getSchema() {
        Map<String, Object> schema = new HashMap<>();
        // ការកំណត់ស្វ័យរូបបទ...
        return schema;
    }
    
    @Override
    public ToolResponse execute(ToolRequest request) {
        try {
            String location = request.getParameters().get("location").asText();
            int days = request.getParameters().has("days") ? 
                request.getParameters().get("days").asInt() : 3;
            
            // ហៅ API អាកាសធាតុ
            Map<String, Object> forecast = getForecast(location, days);
            
            // ស្ថាបនាការឆ្លើយតប
            return new ToolResponse.Builder()
                .setResult(forecast)
                .build();
        } catch (Exception ex) {
            throw new ToolExecutionException("Weather forecast failed: " + ex.getMessage(), ex);
        }
    }
    
    private Map<String, Object> getForecast(String location, int days) {
        // អនុវត្តន៍នឹងហៅ API អាកាសធាតុ
        // ឧទាហរណ៍ដែលបានសម្រួល
        Map<String, Object> result = new HashMap<>();
        // បន្ថែមទិន្នន័យព្យាករណ៍...
        return result;
    }
}

// ស្ថាបនា និងផ្សព្វផ្សាយដោយប្រើ Maven
// mvn clean package
// mvn deploy
```

#### ឧទាហរណ៍ Python: បោះពុម្ពកញ្ចប់ PyPI

```python
# រចនាសម្ព័ន្ធថតសម្រាប់ផាគច PyPI:
# mcp_nlp_tools/
# ├── LICENSE
# ├── README.md
# ├── setup.py
# ├── mcp_nlp_tools/
# │   ├── __init__.py
# │   ├── sentiment_tool.py
# │   └── translation_tool.py

# ឧទាហរណ៍ setup.py
"""
from setuptools import setup, find_packages

setup(
    name="mcp_nlp_tools",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "mcp_server>=1.0.0",
        "transformers>=4.0.0",
        "torch>=1.8.0"
    ],
    author="Your Name",
    author_email="your.email@example.com",
    description="MCP tools for natural language processing tasks",
    long_description=open("README.md").read(),
    long_description_content_type="text/markdown",
    url="https://github.com/username/mcp_nlp_tools",
    classifiers=[
        "Programming Language :: Python :: 3",
        "License :: OSI Approved :: MIT License",
        "Operating System :: OS Independent",
    ],
    python_requires=">=3.8",
)
"""

# ឧទាហរណ៍ការអនុវត្ត​ឧបករណ៍ NLP (sentiment_tool.py)
from mcp_tools import Tool, ToolRequest, ToolResponse, ToolExecutionException
from transformers import pipeline
import torch

class SentimentAnalysisTool(Tool):
    """MCP tool for sentiment analysis of text"""
    
    def __init__(self, model_name="distilbert-base-uncased-finetuned-sst-2-english"):
        # បញ្ចូលម៉ូដែលវិភាគ​អារម្មណ៍
        self.sentiment_analyzer = pipeline("sentiment-analysis", model=model_name)
    
    def get_name(self):
        return "sentimentAnalysis"
        
    def get_description(self):
        return "Analyzes the sentiment of text, classifying it as positive or negative"
    
    def get_schema(self):
        return {
            "type": "object",
            "properties": {
                "text": {
                    "type": "string", 
                    "description": "The text to analyze for sentiment"
                },
                "includeScore": {
                    "type": "boolean",
                    "description": "Whether to include confidence scores",
                    "default": True
                }
            },
            "required": ["text"]
        }
    
    async def execute_async(self, request: ToolRequest) -> ToolResponse:
        try:
            # ដកស្រង់ប៉ារ៉ាម៉ែត្រ
            text = request.parameters.get("text")
            include_score = request.parameters.get("includeScore", True)
            
            # វិភាគអារម្មណ៍
            sentiment_result = self.sentiment_analyzer(text)[0]
            
            # ទ្រង់ទ្រាយលទ្ធផល
            result = {
                "sentiment": sentiment_result["label"],
                "text": text
            }
            
            if include_score:
                result["score"] = sentiment_result["score"]
            
            # ត្រឡប់លទ្ធផល
            return ToolResponse(result=result)
            
        except Exception as e:
            raise ToolExecutionException(f"Sentiment analysis failed: {str(e)}")

# ដើម្បីផ្សាយបញ្ចេញ:
# python setup.py sdist bdist_wheel
# python -m twine upload dist/*
```

### ចែករំលែកអនុសាសន៍ល្អបំផុត

នៅពេលចែករំលែកឧបករណ៍ MCP ជាមួយសហគមន៍:

1. **ឯកសារបនComple ត**:
   - បញ្ជាក់គោលបំណង, របៀបប្រើ និងឧទាហរណ៍
   - ពន្យល់ព្រិត្តិការនិងតម្លៃត្រឡប់
   - អ្នករក្សាទុកឯកសារផ្សេងៗ

2. **ការដោះស្រាយកំហុស**:
   - អនុវត្តការដោះស្រាយកំហុសយ៉ាងរឹងមាំ
   - ផ្តល់សារ​កំហុស​មាន​ប្រយោជន៍
   - ដោះស្រាយករណីគ្រោះថ្នាក់ដោយយាំបាន

3. **ការពិចារណាប្រសិទ្ធភាព**:
   - បង្កើតប្រសិទ្ធភាពសម្រាប់ល្បឿននិងការប្រើប្រាស់ធនធាន
   - អនុវត្តការចងចាំបណ្ដាញពេលចាំបាច់
   - ពិចារណាពីជម្រើសធំឡើង

4. **សុវត្ថិភាព**:
   - ប្រើកូនសោ API សុវត្ថិភាពនិងការផ្ទៀងផ្ទាត់
   - ពិនិត្យនិងបរិច្ឆេទតម្លើង
   - អនុវត្តការកំណត់កំណត់អត្រាសម្រាប់ការហៅ API ខាងក្រៅ

5. **ការប្រឡង**:
   - រួមបញ្ចូលការតេស្តគ្រប់គ្រង
   - សាកល្បងជាមួយប្រភេទបញ្ចូលផ្សេងៗនិងករណីគ្រោង
   - ឯកសារដំណើរការតេស្ត

## ការសហការសហគមន៍ និងអនុសាសន៍ល្អបំផុត

ការសហការយ៉ាងមានប្រសិទ្ធភាពគឺជាគន្លងសម្រាប់ ប្រព័ន្ធសេដ្ឋកិច្ច MCP ដែលរីកចម្រើន។

### បណ្ដាញទំនាក់ទំនង

- បញ្ហា និងការពិភាក្សា GitHub
- សហគមន៍ Microsoft Tech
- ចំណាត់ Discord និង Slack
- Stack Overflow (ស្លាក: `model-context-protocol` ឬ `mcp`)

### ការត្រួតពិនិត្យកូដ

នៅពេលត្រួតពិនិត្យការរួមចំណែក MCP:

1. **ភាពច្បាស់លាស់**: តើកូដច្បាស់លាស់និងមានឯកសារពេញលេញទេ?
2. **ភាពត្រឹមត្រូវ**: តើវាដំណើរការតាមការរំពឹងទុកទេ?
3. **ភាពស្របគ្នា**: តើវាអនុវត្តតាមច្បាប់គម្រោងទេ?
4. **ភាពពេញលេញ**: តើមានការតេស្ត និងឯកសារយ៉ាងពេញលេញទេ?
5. **សុវត្ថិភាព**: តើមានបញ្ហាសុវត្ថិភាពណាមួយទេ?

### ការគ្រប់គ្រងជំនាន់

នៅពេលអភិវឌ្ឍសម្រាប់ MCP:

1. **ជំនាន់វិធានការ**: រក្សាទុកជំនាន់វិធានការដែលឧបករណ៍របស់អ្នកគាំទ្រ
2. **ឧបករណ៍អតិថិជន**: ពិចារណាអំពីការគាំទ្រខាងក្រោយ
3. **ម៉ាស៊ីនបម្រើ**: តាមដានមគ្គុទេសក៍អនុវត្តម៉ាស៊ីនបម្រើ
4. **ការផ្លាស់ប្ដូរបំផ្លាញ**: ច្បាស់លាស់ឯកសារការផ្លាស់ប្ដូរដែលបំផ្លាញ

## គំរូគម្រោងសហគមន៍: កំណត់បង្ហាញឧបករណ៍ MCP

ការរួមចំណែកសហគមន៍សំខាន់មួយអាចជាការអភិវឌ្ឍកំណត់បង្ហាញសាធារណៈសម្រាប់ឧបករណ៍ MCP។

```python
# គំរូសម្រាប់កម្មវិធីចុះបញ្ជីឧបករណ៍សហគមន៍

from fastapi import FastAPI, HTTPException, Depends
from pydantic import BaseModel, Field, HttpUrl
from typing import List, Optional
import datetime
import uuid

# ម៉ូដែលសម្រាប់ចុះបញ្ជីឧបករណ៍
class ToolSchema(BaseModel):
    """JSON Schema for a tool"""
    type: str
    properties: dict
    required: List[str] = []

class ToolRegistration(BaseModel):
    """Information for registering a tool"""
    name: str = Field(..., description="Unique name for the tool")
    description: str = Field(..., description="Description of what the tool does")
    version: str = Field(..., description="Semantic version of the tool")
    schema: ToolSchema = Field(..., description="JSON Schema for tool parameters")
    author: str = Field(..., description="Author of the tool")
    repository: Optional[HttpUrl] = Field(None, description="Repository URL")
    documentation: Optional[HttpUrl] = Field(None, description="Documentation URL")
    package: Optional[HttpUrl] = Field(None, description="Package URL")
    tags: List[str] = Field(default_factory=list, description="Tags for categorization")
    examples: List[dict] = Field(default_factory=list, description="Example usage")

class Tool(ToolRegistration):
    """Tool with registry metadata"""
    id: uuid.UUID = Field(default_factory=uuid.uuid4)
    created_at: datetime.datetime = Field(default_factory=datetime.datetime.now)
    updated_at: datetime.datetime = Field(default_factory=datetime.datetime.now)
    downloads: int = Field(default=0)
    rating: float = Field(default=0.0)
    ratings_count: int = Field(default=0)

# កម្មវិធី FastAPI សម្រាប់ចុះបញ្ជី
app = FastAPI(title="MCP Tool Registry")

# មូលដ្ឋានទិន្នន័យក្នុងចងចាំសម្រាប់ឧទាហរណ៍នេះ
tools_db = {}

@app.post("/tools", response_model=Tool)
async def register_tool(tool: ToolRegistration):
    """Register a new tool in the registry"""
    if tool.name in tools_db:
        raise HTTPException(status_code=400, detail=f"Tool '{tool.name}' already exists")
    
    new_tool = Tool(**tool.dict())
    tools_db[tool.name] = new_tool
    return new_tool

@app.get("/tools", response_model=List[Tool])
async def list_tools(tag: Optional[str] = None):
    """List all registered tools, optionally filtered by tag"""
    if tag:
        return [tool for tool in tools_db.values() if tag in tool.tags]
    return list(tools_db.values())

@app.get("/tools/{tool_name}", response_model=Tool)
async def get_tool(tool_name: str):
    """Get information about a specific tool"""
    if tool_name not in tools_db:
        raise HTTPException(status_code=404, detail=f"Tool '{tool_name}' not found")
    return tools_db[tool_name]

@app.delete("/tools/{tool_name}")
async def delete_tool(tool_name: str):
    """Delete a tool from the registry"""
    if tool_name not in tools_db:
        raise HTTPException(status_code=404, detail=f"Tool '{tool_name}' not found")
    del tools_db[tool_name]
    return {"message": f"Tool '{tool_name}' deleted"}
```

## ចំណុចសំខាន់ៗ

- សហគមន៍ MCP មានភាពចម្រុះនិងស្វាគមន៍ប្រភេទការរួមចំណែកជាច្រើន
- ការរួមចំណែកទៅ MCP អាចធ្វើបានពីកែលម្អការដឹកនាំមូលដ្ឋានរហូតដល់ឧបករណ៍ផ្ទាល់ខ្លួន
- ធ្វើតាមមគ្គុទេសក៍រួមចំណែក បង្កើនឱកាសក្នុងការទទួលយក PR របស់អ្នក
- ការបង្កើតនិងចែករំលែកឧបករណ៍ MCP គឺជាវិធីមានតម្លៃក្នុងការលើកកម្ពស់ប្រព័ន្ធសេដ្ឋកិច្ច
- ការសហការសហគមន៍គឺសំខាន់សម្រាប់ការរីកចម្រើននិងការកែលម្អ MCP

## លំហាត់

1. កំណត់តំបន់មួយក្នុងប្រព័ន្ធសេដ្ឋកិច្ច MCP ដែលអ្នកអាចរួមចំណែក ដោយផ្អែកលើជំនាញនិងចំណង់ចំណូលចិត្តរបស់អ្នក
2. តាក់តែងឃ្លាំង MCP ហើយរៀបចំបរិយាកាសអភិវឌ្ឍក្នុងកន្លែង
3. បង្កើតការកែលម្អតិចតួច ជួសជុលកំហុស ឬឧបករណ៍ដែលមានអត្ថប្រយោជន៍សម្រាប់សហគមន៍
4. ឯកសារបរិច្ចាគរបស់អ្នកជាមួយតេស្តនិងឯកសារដែលមានគុណភាព
5. ដាក់សំណើ Pull Request ទៅឃ្លាំងដែលសមរម្យ

## ធនធានបន្ថែម

- [គម្រោងសហគមន៍ MCP](https://github.com/topics/model-context-protocol)

---

## តើបន្ទាប់ទៅ?

បន្ទាប់: [មេរៀនពីការទទួលយកដោះដូរដំបូង](../07-LessonsfromEarlyAdoption/README.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**ការបដិសេធ**:
ឯកសារនេះត្រូវបានបម្លែងភាសា ដោយប្រើសេវាបម្លែងភាសា AI [Co-op Translator](https://github.com/Azure/co-op-translator)។ ទោះយើងខ្ញុំមានក្តីប្រាថ្នាឱ្យបានច្បាស់លាស់ តែសូមយល់ដឹងថាការបម្លែងដោយស្វ័យប្រវត្តិក៏អាចមានកំហុសឬភាពមិនត្រឹមត្រូវ។ ឯកសារដើមជាភាសាទីតាំងគួរត្រូវបានគេប្រើជាប្រភពច្បាស់លាស់។ សម្រាប់ព័ត៌មានសំខាន់ៗ សូមណែនាំឱ្យប្រើប្រាស់ការប្រែដោយមនុស្សជំនាញ។ យើងខ្ញុំមិនទទួលខុសត្រូវចំពោះការយល់ច្រឡំ ឬការបកស្រាយខុសបន្ទាប់ពីការប្រើប្រាស់ការបម្លែងនេះនោះទេ។
<!-- CO-OP TRANSLATOR DISCLAIMER END -->
# OAuth2 demo client. OAUTH_CLIENT_SECRET is required and has no default.
demo.oauth.client-id=${OAUTH_CLIENT_ID:mcp-client}
demo.oauth.client-secret=${OAUTH_CLIENT_SECRET}

# Changed port to avoid conflict
server.port=8081
